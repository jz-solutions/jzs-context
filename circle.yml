machine:
  node:
   version: 6

dependencies:
  pre:
    - echo "//registry.npmjs.org/:_authToken=${NPM_TOKEN}" > ~/.npmrc
  override:
   - npm install

deployment:
  npm:
    branch: master
    commands:
      - npm run ci-publish

