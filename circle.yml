machine:
  node:
   version: 6

dependencies:
  pre:
    - echo "//registry.npmjs.org/:_authToken=${NPM_TOKEN}" > ~/.npmrc
  override:
   - npm install

test:
  override:
    - npm run ci:build

deployment:
  npm:
    branch: master
    commands:
      - npm run deploy

